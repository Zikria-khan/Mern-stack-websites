{
  "version": 2,
  "build": {
    "src": "package.json",
    "use": "@vercel/static-build",
    "config": {
      "distDir": "dist", 
      "buildCommand": "npm run build"  
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
