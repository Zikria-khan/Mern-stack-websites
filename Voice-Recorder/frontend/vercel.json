{
  "version": 2,
  "build": {
    "use": "@vercel/static-build",
    "config": {
      "distDir": "Voice-Recorder/frontend/dist",
      "buildCommand": "npm run build"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
