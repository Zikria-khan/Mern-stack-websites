{
  "version": 2,
  "build": {
    "src": "Voice-Recorder/frontend/package.json",
    "use": "@vercel/static-build",
    "config": {
      "distDir": "Voice-Recorder/frontend/my/dist",
      "buildCommand": "npm run build"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
